# 🔥 Ignore all generated files and system metadata
target/
build/
.idea/
.DS_Store
*.log

# ✅ Allow project-specific TOML files inside src folders (e.g. src/employer_registry/employer_registry.toml)
!src/**/*.toml

# ✅ Allow the top-level coordinator program TOML
!coordinator_program.toml

# 🛠️ Ignore all other TOML files not explicitly allowed
*.toml

# ✅ Ensure .leo source files are included
!src/**/main.leo

# 🚀 Ignore compiled binary output and cache during development
*.aleo
*.avm
*.r1cs
*.pk
*.vk
*.wasm
*.gz
*.bak
*.lock
*.json

# 🛠️ Ignore editor-specific files
.vscode/
*.iml
*.swp
*.swo
*.swn
*.sublime-project
*.sublime-workspace

# 🔑 Ignore local environment & secrets
.env
.env.local
.env.*.local
.secret
credentials.json

# 🛠️ Ignore log/debug files
logs/
debug.log
npm-debug.log
yarn-debug.log
yarn-error.log

# 🔥 GitHub Actions temp/cache files
.github/workflows/*.log
.github/workflows/*.tmp

# ✅ Preserve empty import folders
src/**/import/*
!.gitkeep

# 📌 POST-BROADCAST NOTE:
# After programs are deployed to Testnet/Mainnet,
# remove *.avm and *.json from this ignore list to track final broadcast outputs,
# and optionally remove `leo clean` from deploy scripts.
